{
  "platform": "windows",
  "queries": {
    "Processes_run_without_binary_disk": {
      "query": "SELECT name, path, pid FROM processes WHERE on_disk = 0;",
      "interval": 300,
      "description": "Detect Processes running without a binary on disk"
        }
    }
}

